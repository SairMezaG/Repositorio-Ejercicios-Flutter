name: tu_nombre_de_proyecto
description: A new Flutter project.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Dependencias adicionales
  get: ^4.6.5 # Asegúrate de usar la última versión disponible

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  
